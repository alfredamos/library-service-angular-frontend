export const baseUrl = 'http://localhost:3100/api';

const baseUrlAuth = `${baseUrl}/auth`;

export const loginUrl = `${baseUrlAuth}/login`;

export const changePasswordUrl = `${baseUrlAuth}/change-password`;
export const editProfileUrl = `${baseUrlAuth}/edit-profile`;
export const signupUrl = `${baseUrlAuth}/signup`;
